import React, { useEffect, useState } from 'react';
import api from '../services/api';

// Similar to ProductManager but for admins

const AdminManager: React.FC = () => {
  // Implement CRUD for admins similar to above
  return (
    <div className="card">
      <h3 className="text-xl font-bold mb-2">Manage Admins</h3>
      {/* Form and list */}
    </div>
  );
};

export default AdminManager;